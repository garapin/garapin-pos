import 'package:flutter/material.dart';

class ConfirmationTransactionWidget extends StatelessWidget {
  const ConfirmationTransactionWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
